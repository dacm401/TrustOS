# Private Beta Readiness Report — Template

> This is a TEMPLATE. Runtime reports are generated by
> `npx tsx scripts/trst/run-private-beta-report.mts` and may be written to
> `docs/private-beta/generated-readiness-report.md` locally. **Runtime reports are not
> committed** (only this template is). Fill the placeholders from the generated output.

---

## Private Beta Readiness Report

- **Generated (timestamp):** `<YYYY-MM-DD HH:MM:SS TZ>`
- **Git branch:** `feature/trst-3-private-beta-readiness`
- **Git commit:** `<short-sha>`

### Validation verdict

- **Overall:** `<READY_WITH_ENV_BLOCKERS | READY | FAIL>`

### Deterministic summary

- `PASS`: `<n>`
- `FAIL`: `<n>`
- Result: `<41 PASS / 0 FAIL>`

### Live summary

- `PASS`: `<n>`
- `ENV_BLOCKED`: `<n>`
- `FAIL`: `<n>`

### Browser harness status

- `<PASS | ENV_BLOCKED (no Chrome) | FAIL>`

### TRST-4H-III status

- `<PASS | ENV_BLOCKED (DB/gateway required) | FAIL>`
- Reason (if blocked): `DB_CONNECTION_REFUSED / GATEWAY_CONFIG_MISSING / ...`

### Known blockers

- `<list from KNOWN_BLOCKERS.md, e.g. TRST-4H-III ×2 ENV_BLOCKED>`

### Next actions

1. `<if ENV_BLOCKED: provide DATABASE_URL + gateway config, re-run npm run validate>`
2. `<if READY: proceed to release checklist>`

---

## Acceptance (see BETA_ACCEPTANCE_CRITERIA.md)

- Private Beta Candidate: allowed `READY_WITH_ENV_BLOCKERS`, requires no real `FAIL`.
- Full READY: requires no `FAIL` and no `ENV_BLOCKED`; TRST-4H-III live `PASS`.
- Rejected: any deterministic/browser `FAIL`, undocumented blocker, or false `READY`.
